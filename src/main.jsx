import React from 'react';
import ReactDOM from 'react-dom/client';
import { AppMyWeather } from './AppMyWeather.jsx';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppMyWeather />
  </React.StrictMode>
);
